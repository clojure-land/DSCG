diagram trie

features
	Trie					: all(	PrefixEncodingType, 
									PrefixLength, 
									PrefixEncodingDirection, 
									PrefixPathCompression?)

	PrefixEncodingType		: one-of(data, hashOfData)
	PrefixLength			: one-of(bounded, unbounded)
	PrefixEncodingDirection	: one-of(leastToMostSignificantBit, mostToLeastSignificantBit)
	
	////
	// Optimizations
	//////
	PrefixPathCompression	: prefixPathCompression